# Profile refresh

The banner is repository-owned SVG, rendered by GitHub as an image. README content uses Markdown and supported HTML; it does not depend on custom CSS or third-party badge services.

Research statements and links were checked against public GitHub and publisher pages on 2026-09-08. The Google Scholar link is user supplied; no citation counts are asserted. The recent-activity markers are retained inside a collapsed section for the existing scheduled workflow. No degree completion, current job title, or private repository is introduced.
